<Window x:Class="pviewer5.FileLoadStatusDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pviewer5"
        mc:Ignorable="d"
        Title="FileLoadStatusDialog" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBox Text="{Binding Path=StatusText}" ScrollViewer.CanContentScroll="True" Height="250" Grid.Row="0"/>
        <Button Content="Dismiss" Click="DismissButtonClick" Grid.Row="1"/>
    </Grid>
</Window>
